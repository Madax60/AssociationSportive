{{ form_start(form) }}
    {% for brochures in evenement.brochureFilename %}
        <div>
            <img src="{{ asset('/uploads/brochures' ~ evenement.brochureFilename)}}" alt="Image évènement" width="150">
        </div>
    {% endfor %}
    <div class="container my-5">
        <button class="btn bg-primary px-3 py-2 rounded text-white">{{ button_label|default('Ajouter') }}</button>
    </div>

{{ form_end(form) }}
