{% extends 'base.html.twig' %}

{% block title %}Evènement{% endblock %}

{% block body %}
    <div class="m-3">
        <a class="btn bg-primary px-3 py-2 rounded text-white" href="{{ path('evenement_index') }}"><i class="fas fa-arrow-left"></i> Retour à la liste</a>
    </div>

    <div class="container m-auto text-center">
        <h1 class="my-5">évènement</h1>

        {{ include('evenement/_form.html.twig', {'button_label': 'Modifier'}) }}

        <a href="{{ path('evenement_index') }}">Retourner à la liste des évènements</a>
    </div>

{% endblock %}
