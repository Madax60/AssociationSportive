{% extends 'base.html.twig' %}

{% block title %}Sport{% endblock %}

{% block body %}
    <div class="m-3">
        <a class="btn bg-primary px-3 py-2 rounded text-white" href="{{ path('sport_index') }}"><i class="fas fa-arrow-left"></i> Retour à la liste</a>
    </div>
    <div class="container m-auto text-center">
        <h1>Supprimer un sport</h1>
        <div class="container m-auto py-5">
            <h3>Id: {{ sport.id }}</h3>
            <h3>Nom: {{ sport.nom }}</h3>
        </div>

        {{ include('sport/_delete_form.html.twig') }}
    </div>

{% endblock %}
